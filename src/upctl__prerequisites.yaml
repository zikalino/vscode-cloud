type: layout-form
id: root
header:
  - type: header
    title: UpCloud Prerequisites
    logo: upcloud.png
form:
  #
  # Install Git
  #
  - type: fieldset
    id: fieldset_esp_idf
    platform: win32
    subitems:
      - type: 'action-row'
        id: install-git
        name: Install Git
        verify: git --version
        install: winget install git

footer:
  - type: footer
    id: footer
    subitems:
      - type: button
        id: install_button
        text: Create
