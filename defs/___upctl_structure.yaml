- upctl account                           # Manage accounts
- upctl account delete                    # Delete a sub-account
- upctl account list                      # List sub-accounts
- upctl account permissions               # Manage permissions
- upctl account permissions list          # List permissions
- upctl account show                      # Show account
- upctl completion                        # Generate the autocompletion script for the specified shell
- upctl completion bash                   # Generate the autocompletion script for bash
- upctl completion fish                   # Generate the autocompletion script for fish
- upctl completion powershell             # Generate the autocompletion script for powershell
- upctl completion zsh                    # Generate the autocompletion script for zsh
- upctl database                          # Manage databases
- upctl database delete                   # Delete a database
- upctl database index                    # Manage database indices
- upctl database index delete             # Delete an index from the specified database.
- upctl database index list               # List current indices of the specified databases
- upctl database list                     # List current databases
- upctl database plans                    # List available plans for given database type
- upctl database properties               # List available properties for given database type
- upctl database session                  # Manage database sessions
- upctl database session cancel           # Terminate client session or cancel running query for a database
- upctl database session list             # List current sessions to specified database
- upctl database show                     # Show database details
- upctl database start                    # Start on a managed database
- upctl database stop                     # Stop a managed database
- upctl database types                    # List available database types
- upctl gateway                           # Manage gateways
- upctl gateway delete                    # Delete a gateway
- upctl gateway list                      # List gateways
- upctl gateway plans                     # List gateway plans
- upctl host                              # Manage private cloud hosts
- upctl host list                         # List private cloud hosts
- upctl ip-address                        # Manage IP addresses
- upctl ip-address assign                 # Assign or create an IP address
- upctl ip-address list                   # List IP addresses
- upctl ip-address modify                 # Modify an IP address
- upctl ip-address remove                 # Remove an IP address
- upctl ip-address show                   # Show current IP address
- upctl kubernetes                        # Manage Kubernetes clusters
- upctl kubernetes config                 # Output Kubernetes cluster kubeconfig
- upctl kubernetes create                 # Create a Kubernetes cluster
- upctl kubernetes delete                 # Delete a Kubernetes cluster
- upctl kubernetes list                   # List current Kubernetes clusters
- upctl kubernetes modify                 # Modifiy an existing cluster
- upctl kubernetes nodegroup              # Manage cluster node-groups
- upctl kubernetes nodegroup create       # Create a new node group into the specified cluster.
- upctl kubernetes nodegroup delete       # Delete the node group from the cluster.
- upctl kubernetes nodegroup scale        # Scale the number of nodes in the node group.
- upctl kubernetes nodegroup show         # Show node group details
- upctl kubernetes plans                  # List available cluster plans
- upctl kubernetes show                   # Show Kubernetes cluster details
- upctl kubernetes versions               # List available versions for Kubernetes clusters
- upctl loadbalancer                      # Manage load balancers
- upctl loadbalancer delete               # Delete a load balancer
- upctl loadbalancer list                 # List current load balancers
- upctl loadbalancer plans                # List available load balancer plans
- upctl loadbalancer show                 # Show load balancer details
- upctl network                           # Manage networks
- upctl network create                    # Create a network
- upctl network delete                    # Delete a network
- upctl network list                      # List networks, by default private networks only
- upctl network modify                    # Modify a network
- upctl network show                      # Show network details
- upctl networkpeering                    # Manage network peerings
- upctl networkpeering delete             # Delete a network peering
- upctl networkpeering disable            # Disable a network peering
- upctl networkpeering list               # List network peerings
- upctl objectstorage                     # Manage managed object storage services
- upctl objectstorage delete              # Delete a Managed object storage service
- upctl objectstorage list                # List current Managed object storage services
- upctl objectstorage regions             # List objectstorage regions
- upctl objectstorage show                # Show Managed object storage service details
- upctl router                            # Manage routers
- upctl router create                     # Create a router
- upctl router delete                     # Delete a router
- upctl router list                       # List routers
- upctl router modify                     # Modify a router
- upctl router show                       # Show current router
- upctl server                            # Manage servers
- upctl server create                     # Create a new server
- upctl server delete                     # Delete a server
- upctl server eject                      # Eject a CD-ROM from the server
- upctl server firewall                   # Manage server firewall rules. Enabling or disabling the firewall is done in server modify.
- upctl server firewall create            # Create a new firewall rule
- upctl server firewall delete            # Removes a firewall rule from a server. Firewall rules must be removed individually. The positions of remaining firewall rules will be adjusted after a rule is removed.
- upctl server firewall show              # Show server firewall details.
- upctl server list                       # List current servers
- upctl server load                       # Load a CD-ROM into the server
- upctl server modify                     # Modifies the configuration of an existing server
- upctl server network-interface          # Manage network interface
- upctl server network-interface create   # Create a network interface
- upctl server network-interface delete   # Delete a network interface
- upctl server network-interface modify   # Modify a network interface
- upctl server plans                      # List server plans
- upctl server restart                    # Restart a server
- upctl server show                       # Show server details
- upctl server start                      # Start a server
- upctl server stop                       # Stop a server
- upctl server storage                    # Manage server storages
- upctl server storage attach             # Attach a storage as a device to a server
- upctl server storage detach             # Detaches a storage resource from a server
- upctl servergroup                       # Manage server groups
- upctl servergroup create                # Create a server group
- upctl servergroup delete                # Delete a server group
- upctl servergroup list                  # List current server groups
- upctl servergroup modify                # Modify a server group
- upctl servergroup show                  # Show server group details
- upctl storage                           # Manage storages
- upctl storage backup                    # Manage backups
- upctl storage backup create             # Create backup of a storage
- upctl storage backup restore            # Restore backup of a storage
- upctl storage clone                     # Clone a storage
- upctl storage create                    # Create a storage
- upctl storage delete                    # Delete a storage
- upctl storage import                    # Import a storage from external or local source
- upctl storage list                      # List current storages
- upctl storage modify                    # Modify a storage
- upctl storage show                      # Show storage details
- upctl storage templatise                # Templatise a storage
- upctl version                           # Display software information
- upctl zone                              # Display zone information
- upctl zone list                         # List available zones
