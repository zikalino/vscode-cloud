- linode account
- linode account cancel                        # Cancel your account
- linode account client-create                 # Create an OAuth client
- linode account client-delete                 # Delete an OAuth client
- linode account client-reset-secret           # Reset an OAuth client secret
- linode account client-update                 # Update an OAuth client
- linode account client-view                   # Get an OAuth client
- linode account clients-list                  # List OAuth clients
- linode account enable-managed                # Enable Linode Managed
- linode account get-account-availability      # Get a region's service availability
- linode account get-availability              # List available services
- linode account invoice-items                 # List invoice items
- linode account invoice-view                  # Get an invoice
- linode account invoices-list                 # List invoices
- linode account login-view                    # Get an account login
- linode account logins-list                   # List user logins
- linode account maintenance-list              # List maintenances
- linode account notifications-list            # List notifications
- linode account payment-create                # Make a payment
- linode account payment-view                  # Get a payment
- linode account payments-list                 # List payments
- linode account promo-add                     # Add a promo credit
- linode account settings                      # Get account settings
- linode account settings-update               # Update account settings
- linode account transfer                      # Get network usage
- linode account update                        # Update your account
- linode account view                          # Get your account

- linode betas
- linode betas enroll                          # Enroll in a Beta program
- linode betas enrolled                        # List enrolled Beta programs
- linode betas enrolled-view                   # Get an enrolled Beta program
- linode betas list                        # List Beta programs
- linode betas view                            # Get a Beta program

- linode child-account
- linode child-account create                  # Create a proxy user token
- linode child-account list                # List child accounts
- linode child-account view                    # Get a child account
- linode databases
- linode databases engine-view                 # Get a managed database engine
- linode databases engines                     # List managed database engines
- linode databases list                    # List managed databases
- linode databases mysql-backup-delete         # Delete a managed MySQL database backup
- linode databases mysql-backup-restore        # Restore a managed MySQL database backup
- linode databases mysql-backup-snapshot       # Create a managed MySQL database backup snapshot
- linode databases mysql-backup-view           # Get a managed MySQL database backup
- linode databases mysql-backups-list          # List managed MySQL database backups
- linode databases mysql-create                # Create a managed MySQL database
- linode databases mysql-creds-reset           # Reset managed MySQL database credentials
- linode databases mysql-creds-view            # Get managed MySQL database credentials
- linode databases mysql-delete                # Delete a managed MySQL database
- linode databases mysql-list                  # List managed MySQL databases
- linode databases mysql-patch                 # Patch a managed MySQL database
- linode databases mysql-ssl-cert              # Get a managed MySQL database SSL certificate
- linode databases mysql-update                # Update a managed MySQL database
- linode databases mysql-view                  # Get a managed MySQL database
- linode databases postgresql-backup-delete    # Delete a managed PostgreSQL database backup
- linode databases postgresql-backup-restore   # Restore a managed PostgreSQL database backup
- linode databases postgresql-backup-snapshot  # Create a managed PostgreSQL database backup snapshot
- linode databases postgresql-backup-view      # Get a managed PostgreSQL database backup
- linode databases postgresql-backups-list     # List managed PostgreSQL database backups
- linode databases postgresql-create           # Create a managed PostgreSQL database
- linode databases postgresql-creds-reset      # Reset managed PostgreSQL database credentials
- linode databases postgresql-creds-view       # Get managed PostgreSQL database credentials
- linode databases postgresql-delete           # Delete a managed PostgreSQL database
- linode databases postgresql-list             # List managed PostgreSQL databases
- linode databases postgresql-patch            # Patch a managed PostgreSQL database
- linode databases postgresql-ssl-cert         # Get a managed PostgreSQL database SSL certificate
- linode databases postgresql-update           # Update a managed PostgreSQL database
- linode databases postgresql-view             # Get a managed PostgreSQL database
- linode databases type-view                   # Get a managed database type
- linode databases types                       # List managed database types
- linode domains
- linode domains clone                         # Clone a domain
- linode domains create                        # Create a domain
- linode domains delete, rm                    # Delete a domain
- linode domains import                        # Import a domain
- linode domains list                      # List domains
- linode domains records-create                # Create a domain record
- linode domains records-delete                # Delete a domain record
- linode domains records-list                  # List domain records
- linode domains records-update                # Update a domain record
- linode domains records-view                  # Get a domain record
- linode domains update                        # Update a domain
- linode domains view                          # Get a domain
- linode domains zone-file                     # Get a domain zone file
- linode events
- linode events list                       # List events
- linode events mark-read                      # Mark an event as read
- linode events mark-seen                      # Mark an event as seen
- linode events view                           # Get an event
- linode firewalls
- linode firewalls create                      # Create a firewall
- linode firewalls delete, rm                  # Delete a firewall
- linode firewalls device-create               # Create a firewall device
- linode firewalls device-delete               # Delete a firewall device
- linode firewalls device-view                 # Get a firewall device
- linode firewalls devices-list                # List firewall devices
- linode firewalls list                    # List firewalls
- linode firewalls rules-list                  # List firewall rules
- linode firewalls rules-update                # Update firewall rules
- linode firewalls update                      # Update a firewall
- linode firewalls view                        # Get a firewall
- linode images
- linode images create                         # Create an image
- linode images delete, rm                     # Delete an image
- linode images list                       # List images
- linode images replicate                      # Replicate an image
- linode images update                         # Update an image
- linode images upload                         # Upload an image
- linode images view                           # Get an image
- linode kernels
- linode kernels list                      # List kernels
- linode kernels view                          # Get a kernel
- linode linodes
- linode linodes backup-restore                # Restore a backup                                       
- linode linodes backup-view                   # Get a backup                                           
- linode linodes backups-cancel                # Cancel backups                                         
- linode linodes backups-enable                # Enable backups                                         
- linode linodes backups-list                  # List backups                                           
- linode linodes boot                          # Boot a Linode                                          
- linode linodes clone                         # Clone a Linode                                         
- linode linodes config-create                 # Create a config profile                                
- linode linodes config-delete                 # Delete a config profile                                
- linode linodes config-interface-add          # Add a configuration profile interface                  
- linode linodes config-interface-delete       # Delete a configuration profile interface               
- linode linodes config-interface-update       # Update a configuration profile interface               
- linode linodes config-interface-view         # Get a configuration profile interface                  
- linode linodes config-interfaces-list        # List configuration profile interfaces                  
- linode linodes config-interfaces-ls
- linode linodes config-interfaces-order       # Reorder configuration profile interfaces               
- linode linodes config-update                 # Update a config profile                                
- linode linodes config-view                   # Get a config profile                                   
- linode linodes configs-list                  # List config profiles                                   
- linode linodes create                        # Create a Linode                                        
- linode linodes delete, rm                    # Delete a Linode                                        
- linode linodes disk-clone                    # Clone a disk                                           
- linode linodes disk-create                   # Create a disk                                          
- linode linodes disk-delete                   # Delete a disk                                          
- linode linodes disk-reset-password           # Reset a disk root password                             
- linode linodes disk-resize                   # Resize a disk                                          
- linode linodes disk-update                   # Update a disk                                          
- linode linodes disk-view                     # Get a disk                                             
- linode linodes disks-list                    # List disks                                             
- linode linodes firewalls-list                # List a Linode's firewalls                              
- linode linodes ip-add                        # Allocate an IPv4 address                               
- linode linodes ip-delete                     # Delete an IPv4 address                                 
- linode linodes ip-update                     # Update an IP address's RDNS for a Linode               
- linode linodes ip-view                       # Get a Linode's IP address                              
- linode linodes ips-list                      # Get networking information                             
- linode linodes linode-reset-password         # Reset a Linode's root password                         
- linode linodes list                      # List Linodes                                           
- linode linodes migrate                       # Initiate a DC migration/pending host migration         
- linode linodes nodebalancers                 # List Linode NodeBalancers                              
- linode linodes reboot                        # Reboot a Linode                                        
- linode linodes rebuild                       # Rebuild a Linode                                       
- linode linodes rescue                        # Boot a Linode into rescue mode                         
- linode linodes resize                        # Resize a Linode                                        
- linode linodes shutdown                      # Shut down a Linode                                     
- linode linodes snapshot                      # Create a snapshot                                      
- linode linodes transfer-view                 # Get a network transfer                                 
- linode linodes type-view                     # Get a type                                             
- linode linodes types                         # List types                                             
- linode linodes update                        # Update a Linode                                        
- linode linodes upgrade                       # Upgrade a Linode                                       
- linode linodes view                          # Get a Linode                                           
- linode linodes volumes                       # List a Linode's volumes                            
- linode lke
- linode lke api-endpoints-list                # List Kubernetes API endpoints
- linode lke cluster-acl-delete                # Delete the control plane access control list
- linode lke cluster-acl-update                # Update the control plane access control list
- linode lke cluster-acl-view                  # Get the control plane access control list
- linode lke cluster-create                    # Create a Kubernetes cluster
- linode lke cluster-dashboard-url             # Get a Kubernetes cluster dashboard URL
- linode lke cluster-delete                    # Delete a Kubernetes cluster
- linode lke cluster-nodes-recycle             # Recycle cluster nodes
- linode lke cluster-update                    # Update a Kubernetes cluster
- linode lke cluster-view                      # Get a Kubernetes cluster
- linode lke clusters-list                     # List Kubernetes clusters
- linode lke kubeconfig-delete                 # Delete a Kubeconfig
- linode lke kubeconfig-view                   # Get a Kubeconfig
- linode lke node-delete                       # Delete a node
- linode lke node-recycle                      # Recycle a node
- linode lke node-view                         # Get a node
- linode lke pool-create                       # Create a node pool
- linode lke pool-delete                       # Delete a node pool
- linode lke pool-recycle                      # Recycle a node pool
- linode lke pool-update                       # Update a node pool
- linode lke pool-view                         # Get a node pool
- linode lke pools-list                        # List node pools
- linode lke regenerate                        # Regenerate a Kubernetes cluster
- linode lke service-token-delete              # Delete a service token
- linode lke version-view                      # Get a Kubernetes version
- linode lke versions-list                     # List Kubernetes versions
- linode longview
- linode longview create                       # Create a Longview client
- linode longview delete, rm                   # Delete a Longview client
- linode longview list                     # List Longview clients
- linode longview plan-update                  # Update a Longview plan
- linode longview plan-view                    # Get a Longview plan
- linode longview subscription-view            # Get a Longview subscription
- linode longview subscriptions-list           # List Longview subscriptions
- linode longview update                       # Update a Longview client
- linode longview view                         # Get a Longview client
- linode managed
- linode managed contact-create                        # Create a managed contact
- linode managed contact-delete                        # Delete a managed contact
- linode managed contact-update                        # Update a managed contact
- linode managed contact-view                          # Get a managed contact
- linode managed contacts-list                         # List managed contacts
- linode managed credential-create                     # Create a managed credential
- linode managed credential-revoke                     # Delete a managed credential
- linode managed credential-sshkey-view                # Get a managed SSH key
- linode managed credential-update                     # Update a managed credential
- linode managed credential-update-username-password   # Update a managed credential's username and password
- linode managed credential-view                       # Get a managed credential
- linode managed credentials-list                      # List managed credentials
- linode managed issue-view                            # Get a managed issue
- linode managed issues-list                           # List managed issues
- linode managed linode-setting-update                 # Update a Linode's managed settings
- linode managed linode-setting-view                   # Get a Linode's managed settings
- linode managed linode-settings-list                  # List managed Linode settings
- linode managed service-create                        # Create a managed service
- linode managed service-delete                        # Delete a managed service
- linode managed service-disable                       # Disable a managed service
- linode managed service-enable                        # Enable a managed service
- linode managed service-update                        # Update a managed service
- linode managed service-view                          # Get a managed service
- linode managed services-list                         # List managed services
- linode managed stats-list                            # List managed stats
- linode network-transfer
- linode network-transfer prices                       # List network transfer prices
- linode networking
- linode networking ip-add                             # Allocate an IP address
- linode networking ip-assign                          # Assign IP addresses
- linode networking ip-share                           # Share IP addresses
- linode networking ip-update                          # Update an IP address's RDNS
- linode networking ip-view                            # Get an IP address
- linode networking ips-list                           # List IP addresses
- linode networking v6-pools                           # List IPv6 pools
- linode networking v6-range-create                    # Create an IPv6 range
- linode networking v6-range-delete                    # Delete an IPv6 range
- linode networking v6-range-view                      # Get an IPv6 range
- linode networking v6-ranges                          # List IPv6 ranges
- linode nodebalancers
- linode nodebalancers config-create                   # Create a config
- linode nodebalancers config-delete                   # Delete a config
- linode nodebalancers config-rebuild                  # Rebuild a config
- linode nodebalancers config-update                   # Update a config
- linode nodebalancers config-view                     # Get a config
- linode nodebalancers configs-list                    # List configs
- linode nodebalancers create                          # Create a NodeBalancer
- linode nodebalancers delete, rm                      # Delete a NodeBalancer
- linode nodebalancers firewalls                       # List node balancer firewalls
- linode nodebalancers list                        # List NodeBalancers
- linode nodebalancers node-create                     # Create a node
- linode nodebalancers node-delete                     # Delete a node balancer's node
- linode nodebalancers node-update                     # Update a node
- linode nodebalancers node-view                       # Get a node balancer's node
- linode nodebalancers nodes-list                      # List nodes
- linode nodebalancers update                          # Update a NodeBalancer
- linode nodebalancers view                            # Get a NodeBalancer
- linode object-storage
- linode object-storage cancel                         # Cancel Object Storage
- linode object-storage clusters-list                  # List clusters
- linode object-storage clusters-view                  # Get a cluster
- linode object-storage keys-create                    # Create an Object Storage key
- linode object-storage keys-delete                    # Revoke an Object Storage key
- linode object-storage keys-list                      # List Object Storage keys
- linode object-storage keys-update                    # Update an Object Storage key
- linode object-storage keys-view                      # Get an Object Storage key
- linode object-storage ssl-delete                     # Delete an Object Storage TLS/SSL certificate
- linode object-storage ssl-upload                     # Upload an Object Storage TLS/SSL certificate
- linode object-storage ssl-view                       # Get an Object Storage TLS/SSL certificate
- linode payment-methods
- linode payment-methods add                           # Add a payment method
- linode payment-methods default                       # Set a default payment method
- linode payment-methods delete, rm                    # Delete a payment method
- linode payment-methods list                      # List payment methods
- linode payment-methods view                          # Get a payment method
- linode phone
- linode phone delete                                  # Delete a phone number
- linode phone sms-code-send                           # Send a phone number verification code
- linode phone verify                                  # Verify a phone number
- linode placement
- linode placement assign-linode                       # Assign a placement group
- linode placement group-create                        # Create placement group
- linode placement group-delete, group-rm              # Delete a placement group
- linode placement group-update                        # Update a placement group
- linode placement group-view                          # Get a placement group
- linode placement groups-list, groups-ls              # List placement groups
- linode placement unassign-linode                     # Unassign a placement group
- linode profile
- linode profile app-delete                            # Revoke app access
- linode profile app-view                              # Get an authorized app
- linode profile apps-list                             # List authorized apps
- linode profile device-revoke                         # Revoke a trusted device
- linode profile device-view                           # Get a trusted device
- linode profile devices-list                          # List trusted devices
- linode profile login-view                            # Get a profile's login
- linode profile logins-list                           # List logins
- linode profile tfa-confirm                           # Enable two factor authentication
- linode profile tfa-disable                           # Disable two factor authentication
- linode profile tfa-enable                            # Create a two factor secret
- linode profile token-create                          # Create a personal access token
- linode profile token-delete                          # Revoke a personal access token
- linode profile token-update                          # Update a personal access token
- linode profile token-view                            # Get a personal access token
- linode profile tokens-list                           # List personal access tokens
- linode profile update                                # Update a profile
- linode profile view                                  # Get a profile
- linode regions
- linode regions list                              # List regions
- linode regions list-avail                            # List regions' availability
- linode regions view                                  # Get a region
- linode regions view-avail                            # Get a region's availability
- linode security-questions
- linode security-questions list                   # List security questions
- linode service-transfers
- linode service-transfers accept                      # Accept a service transfer
- linode service-transfers cancel                      # Cancel a service transfer
- linode service-transfers create                      # Request a service transfer
- linode service-transfers list                    # List service transfers
- linode service-transfers view                        # Get a service transfer request
- linode sshkeys
- linode sshkeys  create                               # Add an SSH key
- linode sshkeys delete, rm                            # Delete an SSH key
- linode sshkeys list                              # List SSH keys
- linode sshkeys update                                # Update an SSH key
- linode sshkeys view                                  # Get an SSH key
- linode stackscripts
- linode stackscripts create                           # Create a StackScript
- linode stackscripts delete, rm                       # Delete a StackScript
- linode stackscripts list                         # List StackScripts
- linode stackscripts update                           # Update a StackScript
- linode stackscripts view                             # Get a StackScript
- linode tags
- linode tags create                                   # Create a tag
- linode tags delete, rm                               # Delete a tag
- linode tags list                                 # List tags
- linode tickets
- linode tickets close                                 # Close a support ticket
- linode tickets create                                # Open a support ticket
- linode tickets list                              # List support tickets
- linode tickets replies                               # List replies
- linode tickets reply                                 # Create a reply
- linode tickets view                                  # Get a support ticket
- linode users
- linode users create                                  # Create a user
- linode users delete, rm                              # Delete a user
- linode users list                                # List users
- linode users update                                  # Update a user
- linode users view                                    # Get a user
- linode vlans
- linode vlans list                                # List VLANs
- linode volumes
- linode volumes attach                                # Attach a volume
- linode volumes clone                                 # Clone a volume
- linode volumes create                                # Create a volume
- linode volumes delete, rm                            # Delete a volume
- linode volumes detach                                # Detach a volume
- linode volumes list                              # List volumes
- linode volumes resize                                # Resize a volume
- linode volumes types                                 # List volume types
- linode volumes update                                # Update a volume
- linode volumes view                                  # Get a volume
- linode vpcs
- linode vpcs create                                   # Create a VPC
- linode vpcs delete                                   # Delete a VPC
- linode vpcs ips-all-list, ips-all-ls                 # List VPC IP addresses
- linode vpcs ips-list, ips-ls                         # List a VPC's IP addresses
- linode vpcs list                                 # List VPCs
- linode vpcs subnet-create                            # Create a VPC subnet
- linode vpcs subnet-delete                            # Delete a VPC subnet
- linode vpcs subnet-update                            # Update a VPC subnet
- linode vpcs subnet-view                              # Get a VPC subnet
- linode vpcs subnets-list, subnets-ls                 # List VPC subnets
- linode vpcs update                                   # Update a VPC
- linode vpcs view                                     # Get a VPC
